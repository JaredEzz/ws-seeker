name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'shared/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Flutter 🚀
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.27.0'
          cache: true # Built-in caching for Flutter SDK

      - name: Cache Dart dependencies 📦
        uses: actions/cache@v4
        with:
          path: |
            /home/runner/.pub-cache
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install dependencies (Shared) 📦
        run: |
          cd shared
          dart pub get

      - name: Generate code (Shared) ⚙️
        run: |
          cd shared
          dart run build_runner build --delete-conflicting-outputs

      - name: Install dependencies (Frontend) 📦
        run: |
          cd frontend
          flutter pub get

      - name: Generate code (Frontend) ⚙️
        run: |
          cd frontend
          dart run build_runner build --delete-conflicting-outputs

      - name: Build Web (WASM) 🏗️
        run: |
          cd frontend
          flutter build web --wasm --dart-define=BACKEND_URL=https://ws-seeker-backend-oilyxc6kdq-uc.a.run.app

      - name: Deploy to Firebase Hosting 🚀
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_WS_SEEKER }}'
          channelId: live
          projectId: ws-seeker
