name: croma_shared
description: Shared DTOs, models, and business logic for Croma Wholesale

environment:
  sdk: ^3.6.0

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.25.7
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.8.0
